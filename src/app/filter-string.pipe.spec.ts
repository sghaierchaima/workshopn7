import { FilterStringPipe } from './filter-string.pipe';

describe('FilterStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStringPipe();
    expect(pipe).toBeTruthy();
  });
});
